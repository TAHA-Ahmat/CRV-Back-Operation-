# Environnement
NODE_ENV=development

# Serveur
PORT=3000

# Base de données MongoDB Atlas (PRODUCTION)
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority&appName=<appName>
# Local: mongodb://localhost:27017/crv
MONGO_URI=mongodb+srv://<USERNAME>:<PASSWORD>@crv-prod-cluster.z33jcf3.mongodb.net/?retryWrites=true&w=majority&appName=crv-prod-cluster

# JWT - ✅ ALIGNÉ SUR MAGASIN
# Générer avec : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REMPLACER_PAR_SECRET_GENERE_64_BYTES_HEX
JWT_EXPIRATION=3h

# CORS - URL du frontend
# Développement local : http://localhost:3000
# Production : https://votre-frontend-production.com
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ✅ Google Drive (pour archivage PDF CRV)
GOOGLE_DRIVE_CREDENTIALS_PATH=config/json/ths-crv-operations.json
GOOGLE_DRIVE_FOLDER_ID=ID_DU_DOSSIER_DRIVE_RACINE

# 📧 Notifications Email (OPTIONNEL)
# Configuration SMTP pour envoyer des emails automatiques
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=votre-email@gmail.com
SMTP_PASS=votre-mot-de-passe-app
SMTP_FROM=noreply@ths.com

# Frontend URL (pour liens dans emails)
FRONTEND_URL=http://localhost:3000
